import './bootstrap';
import { initTourCalendar } from './tour-calendar';

// Initialize flatpickr calendars when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    initTourCalendar();
});
